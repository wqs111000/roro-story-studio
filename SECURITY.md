# 安全策略

## 支持范围

当前版本是本地家庭工具。请通过仓库维护者启用的私密漏洞报告渠道提交安全问题；在该渠道不可用前，请不要在公开 Issue 中附带儿童照片、录音、家庭资料、访问令牌或完整日志。

## 网络边界

- 书架浏览接口保持直接可用；工作台的写操作使用家长 PIN 模式时需要登录会话、HttpOnly Cookie、SameSite 和 CSRF 校验。开发模式允许本机工作台免 PIN，但操作会记录为未验证的 `local-development-workbench`。
- 服务默认仅监听 `127.0.0.1`；局域网访问必须显式传入监听地址或设置 `RORO_LISTEN_ADDRESS`，并限制防火墙范围。服务不提供 TLS 或多用户权限模型；只应在可信本机或可信局域网使用。
- Windows 防火墙规则应限制为 `LocalSubnet`。
- 不要配置公网端口转发、反向代理或互联网隧道。
- 如果未来支持公网访问，必须先实现 TLS、明确的多用户权限、速率限制和完整安全审计，并关闭开发模式。

## 书架与版本安全

- 下架是可恢复的书架状态变更，不是删除；历史发布目录和候选内容必须保留。
- 下架/重新上架请求必须提交当前 `release_id`。服务端会拒绝空值或旧版本值，避免浏览器中的旧页面覆盖新版本状态。
- 发布资产通过受限的故事 ID 和根目录校验提供；音频支持 `Range` 响应以减少大文件加载压力。
- 发现儿童照片、录音、家庭资料、访问令牌、Cookie、私钥或本地绝对路径日志泄露时，应立即停止共享并按私密渠道报告。

## 敏感内容

分享令牌按摘要保存，创建/撤销受会话和 CSRF 校验保护，分享入口检查有效期、撤销状态、书架状态、发布版本和内容摘要。但当前书架、原始媒体和 PDF 地址仍可直接访问；`allow_download` 仅控制分享响应中的下载入口，不构成防下载权限。撤销令牌不能撤回已获取的文件，也不会关闭其他浏览入口。此功能仅适用于现有可信网络边界。

以下内容不应进入公开 Git 历史：儿童照片和录音、家庭身份资料、私人故事素材、未经授权的生成媒体、API Key、令牌、Cookie、私钥和本地绝对路径日志。
